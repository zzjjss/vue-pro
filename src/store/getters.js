// export const playlist = state => state.playlist

export default {
  playlist(state) {
    return state.playlist
  },
  playlistSongIndex(state) {
    return state.playlistSongIndex
  },
  songDetail(state) {
    return state.songDetail
  },
  playState(state) {
    return state.playState
  }
}
